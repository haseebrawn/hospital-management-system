@props([
    'title' => 'Nothing here yet',
    'message' => 'This module will be implemented in the next phase.',
])

<div class="card">
    <div class="card-title">{{ $title }}</div>
    <div class="card-subtitle">{{ $message }}</div>
</div>

